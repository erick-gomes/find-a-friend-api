services:
  app:
    image: bitnami/postgresql
    container_name: find_a_friend_db
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=app